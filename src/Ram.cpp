//
// Created by Eshin Griffith on 9/26/17.
//

#include "RAM.h"

class Disk {
    /* need address type?
      __int32 read(address);
      void write(address, __int32 data);
      */
  
  private:
      __int32[1024] array;
      //not sure what else
  
  
  };